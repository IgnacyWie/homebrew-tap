# IgnacyWie Homebrew Tap

Homebrew formulas for IgnacyWie tools.

## Install agent-loop-workflow

```sh
brew tap IgnacyWie/tap
brew install agent-loop-workflow
```

After installation, run:

```sh
agent-loop --help
```
